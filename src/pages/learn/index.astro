---
import PaddedLayout from "@/layouts/PaddedLayout.astro"
import { Main } from "."

if (import.meta.env.PROD) {
  throw new Error("nope i don't want you built yet")
}
---

<PaddedLayout
  maxWidth="max-w-xl [body:has(.z-layer-active)_&]:max-w-5xl transition-[max-width]"
  description="TODO"
  title="TODO"
  og={{
    imageSrc: "TODO",
    imageAlt: "TODO",
    title: "TODO",
  }}
>
  <div
    class="mx-auto flex w-[64rem] max-w-full flex-1 flex-col text-z transition"
  >
    <Main client:only />
  </div>
</PaddedLayout>
